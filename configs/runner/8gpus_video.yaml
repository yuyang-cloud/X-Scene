# @package _global_
defaults:
  - /runner/default_video

runner:
  checkpointing_steps: 10000 # 2500=2h 7000=6h
  validation_steps: 50000
  lr_scheduler: constant_with_warmup
  enable_xformers_memory_efficient_attention: true
