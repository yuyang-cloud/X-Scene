# @package _global_
defaults:
  - _self_  # make sure override

fid:
  resize: ${dataset.back_resize}  # (h, w)
  padding: ${dataset.back_pad}  # left, top, right and bottom
  raw_output: false
  # path relative to `${ROOT}`
  img_gen_dir: work_dirs/results_img

runner:
  validation_index: all
  validation_times: 1

log_root_prefix: ../work_dirs/results_img
show_box: false
